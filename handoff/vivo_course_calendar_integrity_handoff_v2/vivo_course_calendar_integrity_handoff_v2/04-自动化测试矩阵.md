# 自动化测试矩阵

## 一、真实样表完整链路

- [ ] parser 输出136条校内事件。
- [ ] identityKey 唯一数为136，或重复全部显式报错，不允许静默丢弃。
- [ ] parseAndPreview 输出136个 PreviewItem。
- [ ] commit 前未排除事件数与预览一致。
- [ ] 真实兼职样表仍为14条：11待上课、3已结课。

## 二、身份测试

- [ ] 同一周同一课程同一节次、不同星期 → 两个不同 key。
- [ ] 教师变化 → key 不变、hash 变化。
- [ ] 教室变化 → key 不变、hash 变化。
- [ ] 同一课程不同日期 → 不发生 distinctBy 丢失。
- [ ] 重复 key 被标记，不被静默合并。

## 三、取消测试

- [ ] 已存在事件收到 CANCELLED → Diff 为 CANCELLED。
- [ ] CANCELLED commit 删除系统事件并更新 managed status。
- [ ] 不存在事件收到 CANCELLED → 不创建系统事件。
- [ ] 撤销 CANCELLED → 恢复事件且保存新 calendarEventId。

## 四、撤销中断测试

对 CREATE、UPDATE、DELETE 分别测试：

- [ ] Calendar逆操作前中断。
- [ ] Calendar逆操作后、action状态写回前中断。
- [ ] action已REVERTED后中断。
- [ ] 重启/重试后没有重复事件。
- [ ] 任一Calendar失败不会把批次标为UNDONE。

## 五、执行恢复测试

- [ ] PREPARED无动作超时。
- [ ] PLANNED CREATE但系统无事件。
- [ ] PLANNED CREATE但系统已有带token事件。
- [ ] UPDATE系统为before状态。
- [ ] UPDATE系统为after状态。
- [ ] UPDATE系统为第三种状态→NEEDS_REVIEW。
- [ ] DELETE系统事件存在。
- [ ] DELETE系统事件已不存在。
- [ ] recover实际从启动路径调用。

## 六、导入范围测试

- [ ] 导入新学期不把旧学期标成MISSING。
- [ ] 导入兼职局部日期范围不影响范围外事件。
- [ ] 全量快照缺失事件只提示，不自动删除。

## 七、提醒测试

- [ ] 10分钟改为30分钟产生MODIFIED。
- [ ] afterSnapshot包含30分钟。
- [ ] 撤销恢复10分钟提醒。
- [ ] managed contentHash与最终提醒一致。

## 八、数据库约束

- [ ] managed_event冲突不会通过REPLACE改变主键。
- [ ] action对managedEventId的引用保持有效。
- [ ] fallback destructive migration仅限未发布构建。

