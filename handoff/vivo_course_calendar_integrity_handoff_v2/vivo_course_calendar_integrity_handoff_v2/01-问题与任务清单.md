# 问题与任务清单

## P0 阻断项

| ID | 问题 | 当前证据 | 完成标准 |
|---|---|---|---|
| F1 | 校内 identityKey 碰撞 | 136 条解析事件只有 118 个唯一 key，`distinctBy` 丢 18 条 | 解析→预览仍为136条 |
| F2 | 取消状态未进入 DELETE | parser 产生 CANCELLED，但 DiffEngine 不检查状态 | 已存在取消课→CANCELLED；不存在取消课→忽略 |
| F3 | 撤销中断不幂等 | undo 不跳过 REVERTED；DELETE 会重复 insert | 任意动作后中断均可继续 |
| F4 | 恢复器未接入 | 全仓库无 `importManager.recover()` 调用 | App 启动自动扫描并记录结果 |
| F5 | 恢复判断不可靠 | UPDATE 可能误标 DB_APPLIED；DELETE 无中间状态 | CREATE/UPDATE/DELETE 均可核对系统真实状态 |

## P1 完整性项

| ID | 问题 | 完成标准 |
|---|---|---|
| F6 | MISSING 无导入范围 | 只在同学期/日期窗口内计算 missing |
| F7 | 提醒未进入最终哈希/快照 | 改提醒可产生 MODIFIED，撤销能恢复旧提醒 |
| F8 | Calendar 时区字段不统一 | DTSTART/DTEND 解释与 EVENT_TIMEZONE 均使用 Asia/Shanghai |
| F9 | managed_event 使用 REPLACE | 改用明确 insert/update/upsert，避免主键重建 |
| F10 | R8 规则过宽 | Release 运行样表验证通过，收窄可收窄的 dontwarn |

## 当前任务状态修正

| 原任务 | 修正状态 |
|---|---|
| T1 作息配置 | 完成 |
| T2 兼职 ID 精度 | 当前 XSSF 样表完成 |
| T3/T4 身份与哈希 | 结构完成，校内发生键碰撞 |
| T5 持久化 | 基础表结构完成 |
| T6 DiffEngine | 未完成：CANCELLED、范围匹配缺失 |
| T7 撤销 | 正常路径完成，中断重试未完成 |
| T8 恢复 | 框架存在，未接入且逻辑不足 |
| T9 测试 | 45 条通过，但关键失败场景缺失 |
| T10 真机 | 暂停，修复 P0 后继续 |

## 实施顺序

1. F1 校内身份碰撞。
2. F2 取消状态。
3. F3 撤销中断幂等。
4. F4/F5 恢复器接入和重构。
5. F6 导入范围。
6. F7 提醒哈希和快照。
7. F8/F9 小型一致性修复。
8. F10 Release 真机验证。

