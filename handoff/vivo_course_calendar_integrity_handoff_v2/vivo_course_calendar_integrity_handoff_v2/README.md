# 课程日历 App：第二轮完整性交接包

## 基线

- 仓库：`17kevin17/vivio-course-calendar`
- 审查提交：`58bd21983cea826e55c800befbe5e81629dee992`
- 已确认：Debug/Release 构建通过，45 个现有测试通过。
- 当前结论：T1–T5 基础结构基本完成；T6–T8 尚未达到交付标准；T10 真机验收暂停。

## 本轮目标

在继续 vivo 真机导入前，修复以下完整性问题：

1. 校内课程 identityKey 碰撞导致真实样表静默丢失 18 条事件。
2. `CourseStatus.CANCELLED` 没有转换为 `EventState.CANCELLED`。
3. 撤销在中途终止后不能安全重试。
4. `recover()` 未接入启动流程，且无法可靠恢复 Calendar/Room 不一致。
5. MISSING 检测没有学期或日期范围。
6. 用户选择的提醒没有进入最终 contentHash 和快照。
7. Release 仅构建通过，尚未完成 POI 真机运行验证。

## 文档

- `01-问题与任务清单.md`
- `02-事件身份与差异修复.md`
- `03-撤销与故障恢复修复.md`
- `04-自动化测试矩阵.md`
- `05-Release与vivo真机验收.md`
- `06-下一次交接报告模板.md`

## 完成门槛

- 校内真实样表从解析器到预览保持 `136 → 136`，不得被 `distinctBy` 静默丢弃。
- 取消课不会被创建或更新为正常事件。
- 撤销中断后可继续，不重复创建或误删。
- 启动恢复器实际运行，并有中断点测试证明状态可恢复。
- Release 构建在 vivo 真机成功解析两类样表。

